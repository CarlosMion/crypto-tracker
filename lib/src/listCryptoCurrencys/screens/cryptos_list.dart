import 'package:flutter/material.dart';

class CryptosList extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CryptoTracker Pro'),
      ),
    );
  }
}
